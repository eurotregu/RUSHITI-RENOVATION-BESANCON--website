{
  "name": "rushiti-mcp-gbp",
  "version": "0.1.0",
  "private": true,
  "description": "Serveur MCP (Cloudflare Worker) reliant Claude à la fiche Google Business Profile de RUSHITI Rénovation",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "tsc --noEmit",
    "dry-run": "wrangler deploy --dry-run --outdir=.wrangler/dry-run",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.10.3",
    "@modelcontextprotocol/sdk": "1.30.0",
    "agents": "^0.22.0",
    "hono": "^4.13.7",
    "zod": "^4.5.4"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260904.1",
    "typescript": "^5.9.0",
    "wrangler": "^4.129.0"
  }
}
